using System;

namespace Ambermoon.Data.Pyrdacor
{
    public class Class1
    {
        /*
        GameData
        GraphicProvider
        TextDictionary
        MapManager
        CharacterManager
        SavegameManager
        SavegameSerializer
        DataNameProvider (needs execData)
        FontProvider (needs execData)
        Render.Cursor / CursorHotspots (needs execData)
        ItemManager

        DataReaders (inherit)
        - Map
        - Tileset
        - Labdata
        - Places
        */
    }
}
